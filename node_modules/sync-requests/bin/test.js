"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const SyncRequests_1 = require("./sync-requests/SyncRequests");
const config = {
    method: "get",
    host: "example.com",
    path: "/",
    port: 80
};
const N = 16;
(async () => {
    const requests = new SyncRequests_1.SyncRequests();
    for (let i = 0; i < N; ++i)
        requests.addRequest(config);
    console.log("Starting requests");
    await requests.start();
    console.log("Terminating requests");
    await requests.terminate();
    console.log("OK");
})();
