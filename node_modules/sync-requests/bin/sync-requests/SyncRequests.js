"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const SyncRequest_1 = require("./SyncRequest");
class SyncRequests {
    constructor() {
        this.requests = [];
        this.started = false;
    }
    addRequest(config) {
        if (this.started)
            throw new Error("Request already sent");
        this.requests.push(new SyncRequest_1.SyncRequest(config));
    }
    async start() {
        if (this.started)
            throw new Error("Request already sent");
        this.started = true;
        return await Promise.all(this.requests.map(r => r.start()));
    }
    async terminate() {
        return await Promise.all(this.requests.map(r => r.terminate()));
    }
}
exports.SyncRequests = SyncRequests;
