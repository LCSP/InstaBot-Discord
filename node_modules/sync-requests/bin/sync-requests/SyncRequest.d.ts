/// <reference types="node" />
import * as net from "net";
import { SyncRequestConfig } from "./SyncRequestConfig";
export declare class SyncRequest extends net.Socket {
    readonly config: SyncRequestConfig;
    private toSendLast;
    constructor(config: SyncRequestConfig);
    start(): Promise<void>;
    terminate(): Promise<void>;
}
